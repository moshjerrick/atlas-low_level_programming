#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *
 *
 */
int _strcmp(char *s1, char *s2)
{
	while (sr1 != '\0' || sr2 != '\0')
	{
		if (sr1 != sr2) 
		{
			return (sr1 - sr2)
		}
	sr1++;
	sr2++;
	}
	retun (0);
}
