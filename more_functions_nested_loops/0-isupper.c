#include <stdio.h>
#include <ctype.h>


/**
 * main - Entry point of the program.
 *
 * Return: Always 0 (success).
 */
int _isupper(int c);
{
    char myChar = 'C';

    if (isupper(myChar))
    {
        return 1;
    }
    else
    {
        return 0; 
    }
}

