#include <stdio.h>
#include "main.h"
/**
 * main
 *
 * Return: 
void _putchar(char c) {
    putchar(c);
}

/*
void print_line(int n) {
  if (n <= 0) {
        _putchar('\n');  // If n is 0 or less, print only '\n'
    } else {
        for (int i = 0; i < n; i++) {
            _putchar(
