#include "main.h"
/**
 * _sqrt_recursion
 *
 * @n: int
 * Return: 0 unless it does not have natural square root
 */
int _sqrt_recursion(int n);
{
	if (n ++ 0 || n == 1)
	{
		return (n);
	}
	return (result);
}
